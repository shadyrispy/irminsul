# Patch the two konkers deps to the shadyrispy forks (V70 protos, heuristic
# matching, reflection-based proto JSON for the UI, network/CDN data source).
[patch."https://github.com/konkers/anime-game-data"]
anime-game-data = { git = "https://github.com/shadyrispy/anime-game-data.git", branch = "main" }

[patch."https://github.com/konkers/auto-artifactarium"]
auto-artifactarium = { git = "https://github.com/shadyrispy/auto-artifactarium.git", branch = "feat/heuristic-matching" }
